# Production Environment Variables for Vercel + Supabase
# Note: DATABASE_URL and JWT_SECRET are set in Vercel Dashboard, not here

# API uses relative paths in production (Vercel serverless functions)
# No VITE_API_BASE_URL needed - defaults to /api

# Frontend URL for CORS
VITE_FRONTEND_URL=https://plasticsurgassisstant.vercel.app
